tracker_type: deepsort

# DeepSORT hyperparameters
max_dist: 0.2
min_confidence: 0.3
nms_max_overlap: 1.0
max_iou_distance: 0.7
max_age: 70
n_init: 3
nn_budget: 100

# Feature extractor config (optional depending on implementation)
model_type: mobilenet
use_cuda: True
